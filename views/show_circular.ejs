<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Circulars</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        /* Unique CSS for Show Circulars Table */
        .circulars-table {
            border-collapse: collapse;
            width: 90%;
            margin: auto;
            margin-top: 20px;
            box-shadow: 0 8px 6px -6px #999;
            background: linear-gradient(to right, #fff 49%, #f9f9f9 50%, #fff 51%);
            border-radius: 6px;
            overflow: hidden;
            border: 2px solid black; /* Added black border */
            color: #333;
        }

        .circulars-table th, .circulars-table td {
            padding: 15px;
            text-align: center;
            border: 1px solid black;
            font-weight: 500;
            word-break: break-all;
        }

        .circulars-table th {
            background-color: #4285f4;
            height: 40px;
            color: white;
            position: sticky;
            top: 0;
            z-index: 2;
        }

        .circulars-table tr:hover {
            background-color: #f5f5f5;
        }

        .circulars-table td a {
            text-decoration: none;
            color: #4285f4;
            font-weight: bold;
            transition: color 0.3s ease-in-out;
        }

        .circulars-table td a:hover {
            text-decoration: underline;
            color: #0d47a1;
        }
    </style>
</head>

<body>
    <table class="circulars-table">
        <thead>
            <tr>
                <th>Sr.No</th>
                <th>Date</th>
                <th>Circular Text</th>
                <th>PDF</th>
                <th>View</th>
            </tr>
        </thead>
        <tbody>
            <% for (const circular of circulars) { %>
                <tr>
                    <td><%= circular.circular_id %></td>
                    <td><%= new Date(circular.circular_date).toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' }) %></td>
                    <td><%= circular.circular_text %></td>
                    <td>
                        <% if (circular.pdf) { %>
                            <a href="/download-circular-pdf/<%= circular.circular_id %>">Download</a>
                        <% } else { %>
                            No PDF
                        <% } %>
                    </td>
                    <td>
                        <% if (circular.pdf) { %>
                            <a href="/view-circular-pdf/<%= circular.circular_id %>" target="_blank">View</a>
                        <% } else { %>
                            No PDF
                        <% } %>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
